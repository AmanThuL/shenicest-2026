# 历史资料

这里放已经被当前实现取代、但仍需要保留追溯价值的架构资料。

当前事实以架构根目录和 `systems/`、`contracts/`、`decisions/` 为准；archive 内文档不作为实现依据。

| 文档 | 归档原因 |
|---|---|
| [切片 00 实施计划](切片00_实施计划.md) | 切片 00 的范围、排期与 `D<n>` 决定（含 D21「美术先做占位、正式演出走 Timeline」）。计划已不再驱动当前实现。 |
| [切片 00 场景搭建与接线](切片00_场景搭建与接线.md) | 切片 00 在 Editor 里的建场与接线步骤。 |

**注意**：这两份文档里的 `D<n>` 决定随之失去约束力。按维护约定编号不复用，所以 D21 之类的编号仍然不会被重新分配，但不要再拿它们当作现行规则引用。
