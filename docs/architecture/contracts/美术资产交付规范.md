# 契约：美术资产交付

> 3D 美术 / 技术美术 → gameplay 程序。第一批模型进项目**之前**必须闭环。
> 状态：D14 仍待技术美术三选一确认 · 最后修订 2026-08-24

---

## D14 —— 导出轴向与单位 ⚠️ 唯一未闭环项

Blender 是 Z-up、Unity 是 Y-up，默认导出会让模型在 Unity 里带着 `scale=100` 或 `rotation=-90` 进来。

**三选一，全队只用一种，选完把结论写回本节**：

1. Blender 里导出前 `Object > Apply > All Transforms`，FBX 导出勾 **Apply Transform**；
2. 导出前把物体 X 旋转设为 `-90` 再导；
3. 用 `darktable/blender-to-unity-fbx-exporter` 这类专用导出脚本。

Maya 用户：直接按 cm 单位导 FBX，Unity 侧 Scale Factor 保持 1。

**验收**：模型拖进场景，Transform 是 **position 0 / rotation 0 / scale 1**，且尺寸与真实世界一致（**1 unit = 1 米**）。做不到就是导出错了——**不要在 Unity 侧用父物体套一层旋转去凑**，那会让所有后续的朝向计算都带一个隐藏偏移。

定不下来的代价是整批返工，所以这条排在所有美术工作之前。

> **补充（2026-08-26）：上面三个选项是按静态道具写的，不覆盖蒙皮 + 动画资产。**
>
> 选项 1 的「Apply Transform」在 Blender API 里是 `bake_space_transform`，其官方说明自带警告：
> *"…known to be broken with armatures/animations"*。**带骨骼的资产不能用选项 1。**
>
> Unity 侧的 **Bake Axis Conversion** 是官方文档中唯一明说会处理 animation data 的路径，可作为**选项 4**
> （Blender 保持默认轴向 + 不勾 Apply Transform，Unity 侧打开该开关）。
>
> 证据原文、完整设置清单与验收步骤见 [Blender → Unity 导出管线](../tooling/Blender到Unity导出管线.md)。
> **本节状态不变，仍待技术美术三选一（或四选一）确认。**

---

## D15 —— pivot 与朝向

pivot 放在物体的**接地点**（树、石头、设备的底部中心），正面朝 **+Z**。

---

## D16 —— 碰撞体由 gameplay 程序加，不用 `Generate Colliders`

FBX 导入设置里的 `Generate Colliders` 会给每个 mesh 挂凹面 `MeshCollider`——行走判定不稳、性能差、且美术每次重导都会被重置。

**定案：美术交纯外观 prefab；gameplay 程序把它做成 prefab variant，在 variant 上手加 `BoxCollider` / `CapsuleCollider` / 凸包。**

好处是**美术改外观不影响 gameplay 程序的碰撞与交互组件**，这是整个交接里最省事的一条。

唯一例外：地形本体可以用 Terrain Collider，或美术单独出一份低模碰撞网格。

---

## 命名、目录与导入设置

资产命名与目录归 [`guidelines/02`](../../guidelines/02-project-structure.md)，贴图导入与预算归 [`guidelines/05`](../../guidelines/05-performance.md) 和 [`guidelines/07`](../../guidelines/07-rendering-urp.md)。本文只补模型特有的三条（D14/D15/D16），其余不重复。

---

## 参考

- [Importing model files — Unity Manual](https://docs.unity3d.com/6000.3/Documentation/Manual/ImportingModelFiles.html)
- [Unity & Blender FBX Scale — katsbits](https://www.katsbits.com/codex/unity-blender-fbx-scale/)
- [FBX rotation fix for Unity — katsbits](https://www.katsbits.com/codex/fbx-unity-rotation/)
- [blender-to-unity-fbx-exporter](https://github.com/darktable/blender-to-unity-fbx-exporter)
