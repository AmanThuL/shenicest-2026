# 交互提示与叙事触发规范

全游戏 HUD 提示、交互点文案和叙事触发的行为真源。分两层：**规则**（所有新增交互必须遵守）和**交互点清单**（每个交互点的 intended 行为；`当前` 列与 intended 不一致的就是 bug）。改交互行为先改这里，再改实现。

## 提示管线规则

1. **一条线，一个仲裁者。** 所有 HUD 提示走 `InteractionPrompts.Set/Clear`（`Data/Events/PromptChanged` 通道），按声明的 priority 仲裁，谁都不许直接 raise 通道。
2. **动作提示必须带按键前缀**，格式 `[键] 动词 对象`，例如 `[E] 拾取 手电筒`。这包括 `IInteractable.PromptText` 一族（交谈/调查/采样/识别/观察/查看…）——由 `InteractionProximityTrigger` 在发布时统一加 `[E] ` 前缀，资产文案里**不写**按键（写了就会双重前缀）。
3. **状态描述不冒充动作提示。** "正门被根系和路障封死"这类是观察结果，属于对话/独白/告示内容；动作提示位只出现玩家现在能做的事。
4. **阻塞必须可见。** 一个交互点前置条件不满足时，必须给非空 blocked 文案说清缺什么（例：`需要先拿着手电筒`）。blocked 文案为空 = bug。
5. **每个按键第一次可用时必须教学。** 新按键（F 手电、G 放下、J 扫描、E 交互）在首次可用的场景里要有一次性提示；"玩家拿着手电却不知道要按 F"就是违反本条的实例。
6. **提示与交互条件一致：够近 + 部分入画**，无准星、无瞄准。任何交互不得要求把屏幕中心对准物体。

## 叙事触发规则

1. **占位文案统一为「啦啦啦啦啦啦」**（英文侧带 `(placeholder)` + 资产 id）。它表示"beat 已接线、文案未写"，不是触发 bug；写入正文即自动消失。禁止使用其他占位串。
2. **删内容必须删干净：** 删除一个 DialogueSO / 交互点时，同一提交里删掉引用它的场景触发器和 prefab；场景里指向已删资产的 DialogueTrigger（提示照常显示、按下无反应）= bug。
3. **flag 种入（checkpoint seed / rescue）是静默快照。** 需要在 flag 已种状态下补播的演出必须实现 `IRescueStateRestoredParticipant`（`CueSequence` 已实现：种入了 `m_startOnFlag` 就会补播）。因此**测试档/checkpoint 里绝不许携带结局分支 flag**（`flow.circulation_core/ring/outer`、`flow.chase_started`），否则入场即触发结局演出。
4. **对话资产一次只播一遍**由 `DialogueSO.m_playsOnce` 管；触发器只管"这次触发算不算数"（`m_fireOnce`）。两者语义不许混用。

## 文案位规范

| 位 | 载体 | 规则 |
|---|---|---|
| HUD 交互提示 | PromptChanged 通道 | 上面管线规则 2–4 |
| 头盔 HUD 通知 | HelmetNotice | 系统语气（"外部污染浓度…"），不带按键；伴随的动作提示单独一条 |
| 对话字幕 | DialogueScreen | 说话人：我 / 小花 / 设备 |
| 无线电 | RadioSequence | 只在 00 章外部通讯有效期内 |
| 报告 toast | `官方探索报告已更新` + 计数 | 只在记录实际入库时弹 |

## 交互点清单（intended vs 当前）

状态：✅ 一致 · 🐞 bug · ✎ intended 待策划确认 · 📝 文案未写（占位）

### 00 章 · Main（室外 + 检修通道）

| 交互点 | intended | 当前 | 状态 |
|---|---|---|---|
| 土壤采样 | `[E] 采样 土壤`；出报告 toast | 提示无按键（`采样`） | 🐞 |
| 毯茅/荧光藻识别 | `[E] 识别 <名>` | 提示无按键（`识别`） | 🐞 |
| 不可采样物 | 按 E 后短反馈 `不可采样` | 同 | ✅ |
| 扫描 | `[J] 扫描 <名>` | 同 | ✅ |
| 荧光藻采集 | `[E] 采集 荧光藻`；未持手电 → `需要先拿着手电筒` | 同 | ✅ |
| 地面手电拾取 | `[E] 拾取 手电筒`；**只要拿着+有电+灯没开就一直显示 `[F] 打开手电`**（开灯即隐、关灯再现；低优先级，任何情境提示都压过它）；**不显示"放下"** | 已实现（FlashlightController 直发提示通道） | ✅ |
| 手电电源 | ✎ 待定：设计意图是"采集荧光藻(`flow.flashlight_powered`)之前手电没电"，实现里门禁关着（`m_needsPowerSource: 0`，人人可亮） | 门禁关闭 | ✎🐞 |
| 走廊海报荧光符文 | 手电光照上即显影（纯 shader，无交互键） | 机制正常；因 F 无教学而"看似不亮" | ✅(受连累) |
| 研究所正门 | **无"封死"交互**——场景里根本没有堵门的逻辑，"正门被根系和路障封死"是凭空多出来的文字，整个提示交互删除；00-09 节点只留 DLG-109 独白（文案待写） | prefab 挂着该状态句提示 + DLG-109 占位 | 🐞📝 |
| 检修指示牌 / 研究海报 | `[E] 查看 <物>` + 对应对白 | 提示无按键；DLG-110/111 占位 | 🐞📝 |
| 00 章 flag 触发对白 100–116 | 走到即播 | 已接线；100/101/102/104/105/106/108/109/110/111 占位，112–116 资产缺失(guid 悬空) | 📝🐞 |
| 无线电 RAD-003/004 | 信号中断桥段 | 占位 | 📝 |

### 02 章 · BriggsInterior（实验室）

| 交互点 | intended | 当前 | 状态 |
|---|---|---|---|
| 蓝色烧瓶 | `[E] 拾取 蓝色烧瓶`；**拿起后不显示"放下"**——G 只在需要空手的上下文教（换持提示、密码锁 `[G] 先放下手中的物品`）；符文墙前显示投掷提示 | 常驻 `[G] 放下` 教学已删除 | ✅ |
| 符文墙投掷 | 持瓶：`[E] 将蓝色烧瓶砸向符文`；未持瓶 blocked 文案 | blocked 文案是**空串**（无提示） | 🐞 |
| 密码锁 | `[E] 使用密码锁` → 读入模式提示 → 正确/错误反馈 | 文案自带 `[E]`，与规则 2 的统一前缀冲突（会双前缀） | 🐞 |
| 档案 DOC-001/002 | 进入交互范围出 `[E] 拾取 <名>` 提示；拿起端详时纸面必须可读（阅读态给纸面打光/自发光，不依赖场景灯） | ①范围内**无提示**，玩家不知道能交互；②拿起后光线太暗完全读不了 | 🐞🐞 |
| 初次相遇 DLG-001 | 进入触发 | 已接线有文案有语音 | ✅ |

### 03 章 · GreenhouseInterior（温室）

| 交互点 | intended | 当前 | 状态 |
|---|---|---|---|
| 温室入场 | **进温室不播任何语音**：入场体积只抬 `flow.entered_greenhouse`（供音乐切换与后续 checkpoint 读取）；小花的爆发语气只允许出现在坏结局链里。DLG-004 资产保留，何时何地重挂由策划定 | 生成器已删除入场语音触发并保证重跑不复活 | ✅ |
| 循环终端 | `[E] 查看 终端` → DLG-008 三选一；选 core/ring → 坏结局链，选 outer → 好结局链 | 已接线 | ✅ |
| 坏结局演出 | **只**由玩家在终端选 core/ring 触发；入口/进门绝不触发；测试档不得携带 `flow.circulation_core/ring`、`flow.chase_started`（叙事规则 3，03-04/03-05 测试档属豁免的定点档）；catalog 里 03-01 条目重复两次待清理 | 触发链核验无误（只有 WrongChoiceCore/Ring 能播 DLG-009，只听 circulation 旗） | ✅ |
| 盖娅雕像 | `[E] 端详 雕像` → DLG-006（02-07，面板之前） | ✅ | ✅ |
| 合照 | **02-10 节点，循环面板之前**：`[E] 查看 合照` → DLG-007 对话 | 对话触发器在；另有 DOC-003 照片档案的 prefab 引用残留在 Environment_2 但场景里看不到实体——查明后删除或修复该实例 | 🐞 |
| 牌子 BoundarySign | **已删内容**（DLG-005 已删除）：触发器和提示一并删除，不得给玩家看`查看牌子` | 场景触发器残留，指向已删资产：显示`查看牌子`按下无反应，误导玩家 | 🐞 |
| 水槽（停摆水槽+残留积水） | **02-11 节点，循环面板之前，与结局选择无关**：水槽内静止残留积水是采样对象——挂 `InvestigatableTarget`，提示 `[E] 采样 残留积水`，出报告记录；结局演出只是 CueSequence 把 `WaterFlow` 打开（对话与场景序列 §6） | 只有几何+trigger volume：无 InvestigatableTarget、无提示、连 `残留积水` 的 InvestigationTargetSO 资产都没建 | 🐞 |
