namespace RootsDance.Investigation
{
    /// <summary>Which tool the player uses on a target.</summary>
    public enum InvestigationKind
    {
        /// <summary>采样 — take a physical sample (soil, water).</summary>
        Sample,

        /// <summary>识别 — match against the species database.</summary>
        Identify
    }
}
